# Used only to bootstrap the first persisted platform owner. Keep the real
# value in the hosted environment; it is never sent to the browser.
ADMIN_EMAIL=owner@example.com

# Server-side capability gate for the admin-only demo-data system. Keep it off
# unless an administrator is intentionally preparing an isolated test lot.
DEMO_DATA_ADMIN_ENABLED=false
